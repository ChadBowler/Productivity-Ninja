<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Kalam&family=Merriweather&family=Lilita+One&family=Roboto+Slab&family=Trade+Winds&display=swap" rel="stylesheet">
    <link
      href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css'
      rel='stylesheet'
    />
    <link rel="stylesheet" href="/css/style.css">
    {{!-- {{#each css}}
      <link rel='stylesheet' href='../css/{{this}}' />
    {{/each}} --}}
    <!-- Link to the separate CSS file -->
    <title>Productivity Ninja</title>
  </head>
  <body>
      {{#if logged_in}}
        <div class="main-body">
         {{{body}}}
        </div>
      {{else}}
        <div class="login-body">
          {{{body}}}
        </div>
      {{/if}}
      
      <footer class='text-light text-center py-3 fixed-bottom'>
        {{>footer}}
      </footer>
    </div>
    {{!-- {{#each js}}
      <script src='/js/{{this}}'></script>
    {{/each}} --}}
  </body>
</html>